/**
 * Classe responsável por armazenar as constantes dos tipos de dados aceitos pelo webservice.
 */
export class TipoDeDados {
    static JSON: number = 0; // TiposDeDados.JSON
    static XML: number = 1;  // TiposDeDados.XML
}